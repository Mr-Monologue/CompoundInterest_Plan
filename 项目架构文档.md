# SmartInvest 项目架构文档

## 📋 目录

- [项目概述](#项目概述)
- [技术栈](#技术栈)
- [整体架构](#整体架构)
- [目录结构](#目录结构)
- [核心模块说明](#核心模块说明)
- [数据流](#数据流)
- [API 接口](#api-接口)
- [数据库设计](#数据库设计)
- [部署说明](#部署说明)

---

## 项目概述

SmartInvest 是一个基于网格策略的智能定投系统，支持基金、ETF 等资产的自动化投资建议和交易记录管理。系统采用前后端分离架构，后端使用 FastAPI 提供 RESTful API，前端使用 React + TypeScript 构建单页应用。

### 核心功能

- 🎯 **智能策略引擎**：基于 Z-Score 和网格位置的动态定投策略
- 📊 **实时行情获取**：支持场外基金、ETF、股票等多数据源
- 💰 **全局资金管理**：周预算 + 全局准备金池的智能分配
- 📈 **可视化看板**：实时展示价格走势、MA200、投资建议等
- 📝 **交易记录管理**：完整的买卖记录和持仓统计
- 🔄 **组合调度**：一键执行全组合策略，按低估程度自动分配资金
- 📑 **策略复盘**：生成周报和历史分析报告
- 🏭 **行业穿透分析**：基金持仓穿透，分析行业分布

---

## 技术栈

### 后端技术栈

| 技术 | 版本/说明 | 用途 |
|------|----------|------|
| Python | 3.8+ | 后端开发语言 |
| FastAPI | 最新 | Web 框架，提供 RESTful API |
| SQLModel | 最新 | ORM 框架（基于 SQLAlchemy + Pydantic） |
| SQLite | 内置 | 轻量级数据库 |
| Uvicorn | 最新 | ASGI 服务器 |
| Pandas | 最新 | 数据处理和分析 |
| NumPy | 最新 | 数值计算 |
| AKShare | 最新 | 金融数据获取（A股、基金） |
| yfinance | 最新 | 国际金融数据获取（备用） |
| Requests | 最新 | HTTP 请求库 |

### 前端技术栈

| 技术 | 版本/说明 | 用途 |
|------|----------|------|
| React | 19.2.0 | UI 框架 |
| TypeScript | 5.9.3 | 类型安全 |
| Vite | 7.2.4 | 构建工具和开发服务器 |
| Recharts | 3.4.1 | 数据可视化图表库 |
| Lucide React | 0.554.0 | 图标库 |

---

## 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                            │
│                    (React SPA 应用)                          │
└───────────────────────┬─────────────────────────────────────┘
                        │ HTTP/HTTPS
                        │ RESTful API
┌───────────────────────▼─────────────────────────────────────┐
│                    FastAPI 后端服务                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │   API 路由    │  │  业务逻辑层   │  │  数据访问层    │       │
│  │  (main.py)   │→ │ (services/)  │→ │   (db/)      │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              外部数据源                                │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐          │  │
│  │  │ 东方财富 │  │ 腾讯财经  │  │ Yahoo    │          │  │
│  │  │ AKShare  │  │   API    │  │ Finance  │          │  │
│  │  └──────────┘  └──────────┘  └──────────┘          │  │
│  └──────────────────────────────────────────────────────┘  │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        │ SQL
┌───────────────────────▼─────────────────────────────────────┐
│                    SQLite 数据库                             │
│              (invest.db - 本地文件)                           │
└─────────────────────────────────────────────────────────────┘
```

### 架构特点

1. **前后端分离**：前端独立部署，后端提供 API 服务
2. **静态文件托管**：后端同时托管前端构建产物（`dist` 目录）
3. **分层架构**：API 层 → 业务逻辑层 → 数据访问层
4. **多数据源降级**：市场数据获取支持多个数据源，自动降级
5. **无状态 API**：所有状态存储在数据库中

---

## 目录结构

```
SmartInvest/
├── backend/                          # 后端服务目录
│   ├── __pycache__/                  # Python 字节码缓存
│   │
│   ├── db/                           # 数据库相关模块
│   │   ├── __pycache__/
│   │   ├── database.py               # 数据库连接和初始化
│   │   ├── models.py                 # 数据模型定义（SQLModel）
│   │   └── state.py                  # 全局状态管理模型
│   │
│   ├── services/                     # 业务逻辑层
│   │   ├── __pycache__/
│   │   ├── market.py                 # 市场数据获取（多数据源）
│   │   ├── strategy.py               # 策略计算引擎（网格策略）
│   │   ├── strategy_engine.py        # 策略引擎（旧版，可能已弃用）
│   │   ├── portfolio.py              # 组合调度逻辑
│   │   └── holdings.py               # 基金持仓同步和分析
│   │
│   ├── dist/                         # 前端构建产物（生产环境）
│   │   ├── assets/                   # 静态资源（JS/CSS）
│   │   │   ├── index-*.js
│   │   │   └── index-*.css
│   │   ├── index.html                # 前端入口文件
│   │   └── vite.svg
│   │
│   ├── venv/                         # Python 虚拟环境
│   │   ├── Scripts/                  # Windows 可执行文件
│   │   ├── Lib/                      # Python 库
│   │   └── pyvenv.cfg                # 虚拟环境配置
│   │
│   ├── main.py                       # FastAPI 应用入口（核心文件）
│   ├── invest.db                     # SQLite 数据库文件
│   │
│   ├── init_data.py                  # 初始化数据脚本
│   ├── sync_all.py                   # 批量同步脚本
│   ├── update_industries.py          # 行业数据更新脚本
│   ├── test_fund.py                  # 基金测试脚本
│   ├── test_holdings.py              # 持仓测试脚本
│   └── test_akshare_holdings.py      # AKShare 持仓测试
│
├── frontend/                         # 前端应用目录
│   ├── node_modules/                 # Node.js 依赖包
│   │
│   ├── public/                       # 静态资源目录
│   │   └── vite.svg                  # 公共图标
│   │
│   ├── src/                          # 源代码目录
│   │   ├── assets/                   # 资源文件
│   │   │   └── react.svg
│   │   ├── App.tsx                   # 主应用组件（核心文件）
│   │   ├── App.css                   # 应用样式
│   │   ├── main.tsx                  # 应用入口文件
│   │   └── index.css                 # 全局样式
│   │
│   ├── dist/                         # 构建产物（开发环境）
│   │   ├── assets/
│   │   ├── index.html
│   │   └── vite.svg
│   │
│   ├── index.html                    # HTML 模板
│   ├── package.json                  # 依赖配置
│   ├── package-lock.json             # 依赖锁定文件
│   ├── vite.config.ts                # Vite 配置
│   ├── tsconfig.json                 # TypeScript 配置
│   ├── tsconfig.app.json             # 应用 TypeScript 配置
│   ├── tsconfig.node.json            # Node TypeScript 配置
│   └── eslint.config.js              # ESLint 配置
│
├── README.md                         # 项目说明文档
├── 项目架构文档.md                    # 本文件
├── run.bat                           # Windows 启动脚本
└── 1.txt                             # 临时文件/笔记
```

---

## 核心模块说明

### 后端模块

#### 1. `main.py` - FastAPI 应用入口

**职责**：
- 初始化 FastAPI 应用
- 配置 CORS 跨域
- 定义所有 API 路由
- 托管前端静态文件
- 应用生命周期管理

**关键功能**：
- 数据库表自动创建（启动时）
- API 路由注册（资产管理、交易、策略、组合等）
- 前端 SPA 路由支持（所有非 API 请求返回 `index.html`）
- 静态资源挂载（`/assets` 路径）

**主要路由分组**：
- `/api/assets` - 资产管理
- `/api/transactions` - 交易记录
- `/api/advice/{code}` - 实时投资建议
- `/api/strategy/*` - 策略分析
- `/api/plan/*` - 全局投资计划
- `/api/pool/*` - 资金池管理
- `/api/holdings/*` - 持仓同步
- `/api/analysis/*` - 分析报告

#### 2. `db/database.py` - 数据库连接

**职责**：
- SQLite 数据库连接配置
- 数据库会话管理（Session）
- 表结构初始化

**关键函数**：
- `create_db_and_tables()` - 创建数据库和表
- `get_session()` - 获取数据库会话（依赖注入）

#### 3. `db/models.py` - 数据模型

**数据表定义**：

| 表名 | 说明 | 主要字段 |
|------|------|---------|
| `Asset` | 资产表 | `id`, `code`, `name`, `type`, `max_weight_limit` |
| `Transaction` | 交易表 | `id`, `asset_code`, `date`, `type`, `price`, `amount`, `fee`, `units` |
| `FundState` | 基金状态表 | `id`, `asset_code`, `cumulative_reserve_usage`, `last_signal_date` |
| `DailyPlan` | 每日计划表 | `id`, `asset_code`, `date`, `close`, `ma200`, `dev_pct`, `level`, `base_amt`, `dyn_amt`, `total_amt` |
| `PlanState` | 全局计划表 | `id`, `weekly_budget`, `global_reserve`, `current_week_start`, `budget_used_this_week` |
| `FundHolding` | 基金持仓表 | `id`, `fund_code`, `stock_code`, `stock_name`, `weight`, `industry` |

#### 4. `db/state.py` - 全局状态模型

**职责**：
- 定义全局状态数据模型
- 资金池状态管理

#### 5. `services/market.py` - 市场数据获取

**职责**：
- 从多个数据源获取市场行情
- 数据源降级策略
- 数据缓存机制

**主要函数**：
- `get_strategy_advice(code, name)` - 获取投资建议（包含价格、MA200、历史数据等）
- `fetch_fund_mobile_api(code, use_proxy)` - 东方财富移动端 API
- `fetch_fund_tencent(code)` - 腾讯财经 API
- `fetch_mutual_fund(code)` - 场外基金数据（多源降级）
- `fetch_eastmoney_etf(code)` - 东方财富 ETF 数据
- `fetch_from_yahoo(code)` - Yahoo Finance 数据（备用）

**数据源优先级**：
1. 东方财富 `pingzhongdata` JS 接口
2. 东方财富 `lsjz` 接口
3. 腾讯财经接口
4. Yahoo Finance（ETF/股票）

#### 6. `services/strategy.py` - 策略计算引擎

**职责**：
- 网格策略计算
- 投资建议生成
- 策略复盘报告

**主要函数**：
- `calculate_grid_logic(current_price, ma200, vol_daily, reserve_balance)` - 计算网格位置和投资金额
- `get_instant_analysis(code, session)` - 获取实时分析（价格、MA200、建议等）
- `run_strategy_analysis(code, session)` - 执行策略分析并保存到数据库
- `generate_weekly_report_text(code, session, days)` - 生成周报文本

**策略算法**：
- 网格宽度 = 波动率 × 0.6（最小 0.5%）
- 网格位置 = (当前价格 - MA200) / MA200 / 网格宽度
- 根据网格位置决定投资金额和操作建议

#### 7. `services/portfolio.py` - 组合调度

**职责**：
- 全组合策略执行
- 资金池管理
- 全局状态管理

**主要函数**：
- `run_portfolio_strategy(session)` - 一键执行全组合策略
- `get_global_state(session)` - 获取全局状态
- `adjust_pool_balance(session, amount, operation)` - 调整资金池余额

**组合调度逻辑**：
1. 遍历所有资产，计算网格位置
2. 按低估程度排序
3. 自动分配周预算和准备金
4. 生成交易建议

#### 8. `services/holdings.py` - 基金持仓管理

**职责**：
- 同步基金持仓数据
- 行业穿透分析
- 持仓向量计算

**主要函数**：
- `sync_fund_holdings(session, fund_code)` - 同步基金持仓
- `get_fund_industry_vector(session, fund_code)` - 获取基金行业分布向量
- `fetch_holdings_akshare(code)` - 从 AKShare 获取持仓数据

### 前端模块

#### 1. `src/App.tsx` - 主应用组件

**职责**：
- 应用主界面渲染
- 状态管理（React Hooks）
- API 调用
- 用户交互处理

**主要功能模块**：
- **侧边栏**：资产列表、添加资产、资金池管理
- **主内容区**：
  - 资产详情卡片（价格、MA200、建议等）
  - 价格走势图表（Recharts AreaChart）
  - 持仓统计
  - 交易记录
  - 策略复盘报告
  - 行业分布分析
  - 组合分配可视化

**状态管理**：
- `assets` - 资产列表
- `selectedAsset` - 当前选中资产
- `advice` - 投资建议数据
- `portfolio` - 持仓数据
- `poolState` - 资金池状态
- `industryData` - 行业分布数据

#### 2. `src/main.tsx` - 应用入口

**职责**：
- React 应用初始化
- 根组件挂载

#### 3. `src/App.css` - 样式文件

**职责**：
- 应用全局样式
- 组件样式定义

---

## 数据流

### 1. 获取投资建议流程

```
用户点击资产
    ↓
前端调用 GET /api/advice/{code}
    ↓
后端 main.py 路由处理
    ↓
调用 services/strategy.py::get_instant_analysis()
    ↓
调用 services/market.py::get_strategy_advice() 获取市场数据
    ↓
调用 services/strategy.py::calculate_grid_logic() 计算策略
    ↓
查询数据库获取持仓数据（前十大重仓股）
    ↓
返回 JSON 数据（价格、MA200、建议金额、操作建议、历史数据等）
    ↓
前端渲染图表和卡片
```

### 2. 执行策略流程

```
用户点击"执行策略"
    ↓
前端调用 POST /api/strategy/run/{code}
    ↓
后端调用 services/strategy.py::run_strategy_analysis()
    ↓
获取市场数据 → 计算策略 → 更新 FundState → 保存 DailyPlan
    ↓
返回执行结果
    ↓
前端显示成功提示
```

### 3. 组合调度流程

```
用户点击"执行全组合"
    ↓
前端调用 POST /api/plan/run
    ↓
后端调用 services/portfolio.py::run_portfolio_strategy()
    ↓
遍历所有资产：
  - 获取市场数据
  - 计算网格位置
  - 按低估程度排序
  - 分配资金（周预算 + 准备金）
  - 生成交易建议
    ↓
返回组合调度结果
    ↓
前端显示分配结果和交易建议
```

### 4. 交易记录流程

```
用户填写交易表单
    ↓
前端调用 POST /api/transactions
    ↓
后端计算份额（units = (amount - fee) / price）
    ↓
保存 Transaction 记录
    ↓
如果是从资金池扣款，更新 PlanState.pool_balance
    ↓
返回交易记录
    ↓
前端刷新持仓统计
```

---

## API 接口

### 资产管理

| 方法 | 路径 | 说明 | 请求体 | 响应 |
|------|------|------|--------|------|
| GET | `/api/assets` | 获取所有资产 | - | `Asset[]` |
| POST | `/api/assets` | 添加资产 | `{code, name, max_weight_limit?}` | `Asset` |
| DELETE | `/api/assets/{asset_id}` | 删除资产 | - | `{ok: true}` |

### 行情与建议

| 方法 | 路径 | 说明 | 请求体 | 响应 |
|------|------|------|--------|------|
| GET | `/api/advice/{code}` | 获取实时投资建议 | - | `{current_price, ma200, action, suggested_amount, history, top_holdings, ...}` |

### 交易记录

| 方法 | 路径 | 说明 | 请求体 | 响应 |
|------|------|------|--------|------|
| POST | `/api/transactions` | 记录交易 | `{asset_code, type, price, amount, fee?, date?, from_pool?}` | `Transaction` |
| GET | `/api/transactions/{code}` | 获取交易历史 | - | `Transaction[]` |
| PUT | `/api/transactions/{tx_id}` | 修改交易 | `{price, amount, ...}` | `Transaction` |
| DELETE | `/api/transactions/{tx_id}` | 删除交易 | - | `{ok: true}` |
| GET | `/api/portfolio/{code}` | 获取持仓统计 | - | `{total_units, total_cost, history_count}` |

### 策略分析

| 方法 | 路径 | 说明 | 请求体 | 响应 |
|------|------|------|--------|------|
| POST | `/api/strategy/run/{code}` | 执行策略分析 | - | `{ok: true, ...}` |
| GET | `/api/strategy/report/{code}?days=7` | 获取复盘报告 | - | `{report: string}` |

### 全局计划

| 方法 | 路径 | 说明 | 请求体 | 响应 |
|------|------|------|--------|------|
| GET | `/api/plan/state` | 获取全局状态 | - | `{pool_balance, base_investment, deposit_frequency}` |
| POST | `/api/plan/run` | 执行全组合策略 | - | `{results: [...], ...}` |

### 资金池管理

| 方法 | 路径 | 说明 | 请求体 | 响应 |
|------|------|------|--------|------|
| GET | `/api/pool` | 获取资金池信息 | - | `{pool_balance, base_investment, deposit_frequency}` |
| POST | `/api/pool/deposit` | 资金池充值 | `{amount}` | `{ok: true, new_balance}` |
| POST | `/api/pool/config` | 更新配置 | `{base_investment?, pool_balance?}` | `{ok: true, ...}` |

### 持仓分析

| 方法 | 路径 | 说明 | 请求体 | 响应 |
|------|------|------|--------|------|
| POST | `/api/holdings/sync/{code}` | 同步基金持仓 | - | `{ok: true}` |
| GET | `/api/analysis/industry` | 获取行业分布 | - | `[{name, value, ratio}, ...]` |

---

## 数据库设计

### ER 图（简化）

```
┌─────────────┐         ┌──────────────┐
│   Asset     │◄────────┤ Transaction  │
│             │         │              │
│ - id        │         │ - id         │
│ - code      │         │ - asset_code │
│ - name      │         │ - date       │
│ - type      │         │ - type       │
│ - max_weight│         │ - price      │
└─────────────┘         │ - amount     │
                        │ - units      │
┌─────────────┐         └──────────────┘
│  FundState  │
│             │         ┌──────────────┐
│ - id        │         │  DailyPlan   │
│ - asset_code│◄────────┤              │
│ - cumulative│        │ - id         │
│   _reserve  │         │ - asset_code │
│ - last_signal│        │ - date       │
└─────────────┘         │ - close      │
                        │ - ma200      │
┌─────────────┐         │ - dev_pct    │
│ PlanState   │         │ - level      │
│             │         │ - base_amt   │
│ - id (固定1)│         │ - dyn_amt    │
│ - weekly_   │         │ - total_amt  │
│   budget    │         └──────────────┘
│ - global_   │
│   reserve   │         ┌──────────────┐
│ - current_  │         │ FundHolding  │
│   week_start│         │              │
│ - budget_   │         │ - id         │
│   used      │         │ - fund_code  │
└─────────────┘         │ - stock_code │
                        │ - stock_name │
                        │ - weight     │
                        │ - industry   │
                        └──────────────┘
```

### 表关系说明

- **Asset ↔ Transaction**：一对多（一个资产有多笔交易）
- **Asset ↔ FundState**：一对一（一个资产对应一个状态）
- **Asset ↔ DailyPlan**：一对多（一个资产有多天的计划记录）
- **Asset ↔ FundHolding**：一对多（一个基金有多个持仓）

### 索引设计

- `Asset.code` - 唯一索引（资产代码唯一）
- `Transaction.asset_code` - 索引（按资产查询交易）
- `FundState.asset_code` - 唯一索引
- `DailyPlan.asset_code` 和 `DailyPlan.date` - 复合索引
- `FundHolding.fund_code` - 索引

---

## 部署说明

### 开发环境

#### 后端启动

```bash
cd backend
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt  # 如果有 requirements.txt
python main.py
```

后端服务运行在：`http://0.0.0.0:8000`

#### 前端启动

```bash
cd frontend
npm install
npm run dev
```

前端开发服务器运行在：`http://localhost:5173`

### 生产环境

#### 1. 构建前端

```bash
cd frontend
npm run build
```

构建产物在 `frontend/dist/` 目录

#### 2. 复制构建产物到后端

```bash
# Windows
xcopy /E /I frontend\dist backend\dist

# Linux/Mac
cp -r frontend/dist backend/
```

#### 3. 启动后端（同时托管前端）

```bash
cd backend
python main.py
```

访问：`http://0.0.0.0:8000`

### 注意事项

1. **数据库文件**：`backend/invest.db` 需要定期备份
2. **静态文件路径**：确保 `backend/dist/` 目录存在且包含前端构建产物
3. **端口配置**：可在 `main.py` 中修改端口（默认 8000）
4. **CORS 配置**：生产环境建议限制 `allow_origins`
5. **数据源稳定性**：部分数据源可能需要 VPN，系统已实现自动降级

---

## 扩展开发指南

### 添加新的数据源

1. 在 `backend/services/market.py` 中添加新的 `fetch_*` 函数
2. 在 `get_strategy_advice()` 中添加降级逻辑
3. 测试数据获取和错误处理

### 修改策略算法

1. 编辑 `backend/services/strategy.py::calculate_grid_logic()`
2. 调整网格宽度、投资金额计算公式
3. 更新 `get_instant_analysis()` 以反映新逻辑

### 添加新的 API 接口

1. 在 `backend/main.py` 中添加路由装饰器
2. 实现业务逻辑（可直接在路由中或调用 `services/` 模块）
3. 更新前端 `App.tsx` 调用新接口

### 添加前端功能

1. 在 `frontend/src/App.tsx` 中添加新的状态和 UI 组件
2. 调用后端 API 获取数据
3. 使用 Recharts 或其他库进行数据可视化

---

## 版本历史

- **v2.0** - 重构策略引擎，使用全局准备金池，添加组合调度功能
- **v1.0** - 基础功能：资产管理、行情获取、网格策略、交易记录

---

**文档最后更新**：2025年12月

